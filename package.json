{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@design-system-ignite-ui-caio-queiroz/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1"
  }
}
